# random_password_generator
A Java AWT-based Password Generator using Frame, TextField, Checkbox, Button, SecureRandom, and Clipboard. It lets users choose lowercase, uppercase, digits, and symbols, set length, generate strong passwords, and copy them using Java’s event handling and UI components.
